class Math_try{
    public  static void main(String [] args) {
        double x = 3.14;
        double y = -10;

        double z = Math.abs(y); //10
        double aa = Math.sqrt(z);
        double ab = Math.max(x,y); //3.14
        double ac = Math.ceil(x); //4.0
        double ad = Math.floor(x); //3.0
    }
}